// Simple Object
const student = {
  name: "Tanuja",
  age: 21,
  course: "Web Development"
};

// Using for...in loop to iterate
for (let key in student) {
  console.log(key + " = " + student[key]);
}
